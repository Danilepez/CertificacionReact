name: Save Results
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run_vite:
    runs-on: ubuntu-latest
    steps:
      - name: Download Code
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "24"

      - name: Install Dependencies
        working-directory: clase18/tres-en-raya
        run: npm install

      - name: Run Vite Build
        working-directory: clase18/tres-en-raya
        run: npx vite build
      
      - name: save zip
        uses: actions/upload-artifact@v4
        with:
          name: tic-tac-toe-build
          path: clase18/tres-en-raya/dist